export const initialState = {
  likedPokemons: [],
  currentPokemon: null,
  theme: "light",
};

export const pokemonReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
